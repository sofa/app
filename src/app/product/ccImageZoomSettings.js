'use strict';

angular.module('CouchCommerceApp')
       .factory('ccImageZoomSettings', function () {
    //this is just an empty object so that we have a home
    //for all the different settings which need to be shared
    //across different services.
    return {};
});
